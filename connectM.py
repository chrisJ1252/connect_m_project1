import sys
from game import Game

# TIP: sys.argv will be [script_name, N, M, H] — validate all three before starting
# TIP: constraints to enforce: 3 <= N <= 10, 1 < M <= N, H in {0, 1}
# print a helpful error message if inputs are wrong, then exit

if __name__ == "__main__":
    pass